# Cloudflare Pages Environment Variables
# Copy these to your Cloudflare Pages Settings → Environment variables

# Application Settings
ENVIRONMENT=production
APP_NAME=MedCheck
PYTHON_VERSION=3.11

# Security (IMPORTANT: Generate new values for production!)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=YOUR_SECRET_KEY_HERE_CHANGE_THIS_IN_PRODUCTION

# VAPID Keys for Push Notifications
# Copy from vapid_keys.json
VAPID_PUBLIC_KEY=YOUR_VAPID_PUBLIC_KEY_FROM_VAPID_KEYS_JSON
VAPID_PRIVATE_KEY=YOUR_VAPID_PRIVATE_KEY_FROM_VAPID_KEYS_JSON

# Database (Cloudflare D1)
# This will be configured through D1 Bindings in Cloudflare Pages
# Binding name: DB
# Database: medcheck-db

# CORS Settings (optional - configure if using custom domain)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
