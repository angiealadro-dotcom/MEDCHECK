{
  "name": "medcheck-workers",
  "version": "2.0.0",
  "description": "MedCheck - Sistema de Gestión de Seguridad del Paciente (Cloudflare Workers)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest",
    "db:migrate": "wrangler d1 execute medcheck-db --file=./migrations/schema.sql",
    "db:seed": "wrangler d1 execute medcheck-db --file=./migrations/seed.sql"
  },
  "dependencies": {
    "hono": "^3.11.7",
    "drizzle-orm": "^0.29.1",
    "@hono/zod-validator": "^0.1.11",
    "zod": "^3.22.4",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "date-fns": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.22.1",
    "typescript": "^5.3.3",
    "drizzle-kit": "^0.20.6",
    "vitest": "^1.0.4"
  }
}
