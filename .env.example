# MedCheck Environment Configuration
# Copia este archivo a .env y ajusta los valores según tu entorno

# =============================================================================
# CONFIGURACIÓN DE LA APLICACIÓN
# =============================================================================
APP_NAME=MedCheck
ENVIRONMENT=development  # development, staging, production
DEBUG=True
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# =============================================================================
# CONFIGURACIÓN DEL SERVIDOR
# =============================================================================
HOST=0.0.0.0
PORT=8002
WORKERS=1  # Para desarrollo usar 1, en producción ajustar según CPUs

# =============================================================================
# BASE DE DATOS
# =============================================================================
# SQLite local (desarrollo)
DATABASE_URL=sqlite:///./medcheck.db

# PostgreSQL (producción - ejemplo)
# DATABASE_URL=postgresql://user:password@localhost:5432/medcheck

# Configuración de pool de conexiones
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_RECYCLE=3600

# =============================================================================
# SEGURIDAD Y AUTENTICACIÓN
# =============================================================================
# IMPORTANTE: Genera una clave secreta única para producción
# Ejecuta: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
TOKEN_URL=auth/jwt/login
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================
# Lista de orígenes permitidos separados por coma
CORS_ORIGINS=http://localhost:8002,http://127.0.0.1:8002

# =============================================================================
# ELEVENLABS API (Text-to-Speech)
# =============================================================================
# Obtén tu API key en: https://elevenlabs.io/
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# =============================================================================
# SNOWFLAKE (Data Warehouse - Opcional)
# =============================================================================
SNOWFLAKE_ACCOUNT=tu_cuenta
SNOWFLAKE_USER=tu_usuario
SNOWFLAKE_PASSWORD=tu_password
SNOWFLAKE_DATABASE=MEDCHECK_DB
SNOWFLAKE_SCHEMA=PUBLIC
SNOWFLAKE_WAREHOUSE=tu_warehouse
SNOWFLAKE_ROLE=ACCOUNTADMIN

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 1. NO subas el archivo .env a control de versiones (git)
# 2. Asegúrate de que .env esté en .gitignore
# 3. En producción, usa variables de entorno del sistema o secrets manager
# 4. Cambia SECRET_KEY en producción por una única y segura
